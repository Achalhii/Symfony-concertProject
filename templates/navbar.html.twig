<nav class="navbar navbar-expand-lg navbar-light bg_nav">
    <div class="container-fluid">
        <a class="navbar-brand" href={{ path('concert_list') }}>Home</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href={{ path('concertPast_list') }}>Archives</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href={{ path('bands_list') }}>Groupes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href={{ path('room_list') }}>Salles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href={{ path('organization_list') }}>Organisations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href={{ path('artists_list') }}>Artistes</a>
                </li>
            </ul>
        </div>
        {% if app.user %}
            <form class="form-inline">
                <h5 class="mr-sm-4">Bonjour {{ app.user.getUsername() }} </h5>
                <a  class="mr-sm-4" href={{ path('my_account')}} >Mon Profil</a>
                <a class="mr-sm-4" href={{ path('app_logout')}} >Logout</a>
            </form>
            {% else %}
                <form class="form-inline">
                    <a  class="mr-sm-4" href={{ path('app_login')}} >Login</a>
                </form>
        {% endif %}

    </div>
</nav>